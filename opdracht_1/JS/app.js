/* Schrijf javascript hier */
let firstName = "Henk";
let lastName = "Pietersma";

document.getElementById('innerHTML').innerHTML = `${firstName} ${lastName}`;

document.write(`${firstName} ${lastName}`);

window.alert(`${firstName} ${lastName}`);
